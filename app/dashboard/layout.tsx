import type { Metadata } from 'next';
export const metadata: Metadata = {title:'RetinaReview — Research workspace'};
export default function DashboardLayout({children}: {children: React.ReactNode}) {return children;}
